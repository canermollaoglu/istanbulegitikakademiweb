@using Microsoft.AspNetCore.Identity
@using NitelikliBilisim.Core.Entities
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<header class="header menu_2">
    <div id="preloader"><div data-loader="circle-side"></div></div><!-- /Preload -->
    <div id="logo">
        <a href="index.html"><img src="img/logo.png" width="149" height="42" data-retina="true" alt=""></a>
    </div>
    <ul id="top_menu">
        @if (!SignInManager.IsSignedIn(User))
        {
            <li><a href="@Url.Action("Login","Account")" class="login">Login</a></li>
        }
        else
        {
            <li><a href="@Url.Action("Logout", "Account")" class="login">Login</a></li>
        }
        <li><a href="#0" class="search-overlay-menu-btn">Search</a></li>
        <li class="hidden_tablet"><a href="admission.html" class="btn_1 rounded">Kaydol</a></li>
    </ul>
    <!-- /top_menu -->
    <a href="#menu" class="btn_mobile">
        <div class="hamburger hamburger--spin" id="hamburger">
            <div class="hamburger-box">
                <div class="hamburger-inner"></div>
            </div>
        </div>
    </a>
    <nav id="menu" class="main-menu">
        <ul>
            <li>
                <span><a href="#0">Anasayfa</a></span>
                <ul>
                </ul>
            </li>
            <li>
                <span><a href="#0">Kategoriler</a></span>
                <ul>
                    @*<li><a href="courses-grid.html">Courses grid</a></li>*@
                </ul>
            </li>
            <li>
                <span><a href="#0">Eğitmenler</a></span>
                <ul>

                </ul>
            </li>
            @if (SignInManager.IsSignedIn(User))
            {
                <li>
                    <span><a href="#0">Kurslarım</a></span>
                    <ul>
                    </ul>
                </li>
            }

        </ul>
    </nav>
    <!-- Search Menu -->
    <div class="search-overlay-menu">
        <span class="search-overlay-close"><span class="closebt"><i class="ti-close"></i></span></span>
        <form role="search">
            <input value="" name="q" type="search" placeholder="Ara..." id="input-search" />
            <button type="button" id="btn-search">
                <i class="icon_search"></i>
            </button>
        </form>
    </div>
    <!-- End Search Menu -->
</header>