@model NitelikliBilisim.Core.Entities.groups.GroupExpenseType
@{
    ViewData["Title"] = "Gider Tipi Güncelle";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section styles{
    <link href="~/css/sup/alert-sup.css" rel="stylesheet" />
}
@section alerts{
    <partial name="partials/_Alert" />
}
<div class="row">
    <div class="col-md-6">
        <div class="box_general padding_bottom">
            <div class="header_box version_2">
                <h2><i class="fa fa-file"></i>Gider Tipi Güncelle</h2>
                <input type="hidden" value="@Model.Id" id="expense-type-id" />
            </div>
            <form id="form-update-group-expense-type" style="margin-bottom:50px">
                @Html.AntiForgeryToken()
                <div class="form-group">
                    <label>Gider Tipi</label>
                    <input type="text" class="form-control" autocomplete="off" placeholder="Kategori İsmi" id="input-name" value="@Model.Name">
                </div>
                <div class="form-group">
                    <label>Açıklaması</label>
                    <textarea id="input-description" rows="5" autocomplete="off" class="form-control" style="height:100px;" placeholder="Kategori açıklaması kullanıcılar tarafından görülmeyecektir. Yalnızca admin panelde görülebilir...">@Model.Description</textarea>
                </div>
                <div class="form-group">
                    <button id="btn-save" class="btn btn-outline-primary btn-sm pull-right">Kaydet</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section postScripts{
    <script src="~/js/sup/alert-sup.js"></script>
    <script src="~/js/lib/security-support.js"></script>
    <script src="~/js/areas/admin/admin-group-expense-type-update.js"></script>
}